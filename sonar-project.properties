sonar.projectKey=DEFRA_rod-licencing
sonar.organization=defra

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=Rod Licencing Digital Services
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Coverage stats location
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Excluded paths
sonar.test.exclusions=**/__tests__/**, **/__mocks__/**, **/*.spec.*, **/*.test.*
sonar.coverage.exclusions=**/__tests__/**, **/__mocks__/**, **/*.spec.*, **/*.test.*
