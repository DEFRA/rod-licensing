beforeEach(() => {
  // Set up a completed permit
})

describe('The agreed handler', () => {
  it('Page requests are redirected to the agreed handler when the agree flag is set to true', async () => {})
  it('The agree handler requests to the API multiple times if the API response is not successful', async () => {})
  it('The agree handler requests to the API once with a success response', async () => {})
  it('The agree handler redirects to the completion handler if no payment is required', async () => {})
  it('The agree handler calls the initialize payment once if the GOV.PAY response is successful', async () => {})
  it('The agree handler calls the initialize payment multiple times if the GOV.PAY response is not successful', async () => {})
})
