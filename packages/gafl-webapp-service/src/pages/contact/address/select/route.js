import { ADDRESS_SELECT, CONTROLLER } from '../../../../constants.js'
import pageRoute from '../../../../routes/page-route.js'

export default pageRoute(ADDRESS_SELECT.page, ADDRESS_SELECT.uri, null, CONTROLLER.uri)
