import { ADDRESS_ENTRY, CONTROLLER } from '../../../../constants.js'
import pageRoute from '../../../../routes/page-route.js'

export default pageRoute(ADDRESS_ENTRY.page, ADDRESS_ENTRY.uri, null, CONTROLLER.uri)
